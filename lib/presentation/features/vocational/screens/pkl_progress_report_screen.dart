// Alias agar route yang sudah ada di router tetap bisa dipakai
import 'package:flutter/material.dart';
import 'progres_pkl_screen.dart';

class PklProgressReportScreen extends StatelessWidget {
  const PklProgressReportScreen({super.key});

  @override
  Widget build(BuildContext context) => const ProgresPKLScreen();
}
