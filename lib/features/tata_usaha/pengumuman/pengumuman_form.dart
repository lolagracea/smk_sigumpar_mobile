import 'package:flutter/material.dart';

class PengumumanForm extends StatelessWidget {
  const PengumumanForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      title: Text('Form Pengumuman'),
      content: Text('TODO: form CRUD pengumuman'),
    );
  }
}
