import 'package:flutter/material.dart';
import '../../shared/coming_soon_page.dart';

class CetakPreview extends StatelessWidget {
  const CetakPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const ComingSoonPage(title: 'Cetak Preview');
  }
}
