import 'package:flutter/material.dart';
import '../../shared/coming_soon_page.dart';

class ParentingForm extends StatelessWidget {
  const ParentingForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const ComingSoonPage(title: 'Form Parenting');
  }
}
