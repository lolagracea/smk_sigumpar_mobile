import 'package:flutter/material.dart';

class KeycloakWebviewPage extends StatelessWidget {
  const KeycloakWebviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TODO: implementasi login via WebView / OIDC'),
      ),
    );
  }
}
