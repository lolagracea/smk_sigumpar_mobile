name: flutter_smk_sigumpar_mobile
description: Aplikasi mobile SMK Negeri 1 Sigumpar
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  go_router: ^16.3.0
  dio: ^5.9.2
  http: ^1.6.0
  flutter_secure_storage: ^10.0.0
  intl: ^0.20.2
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true